# Contributing

## Dev Environment

1. Run `make` to build and install the cli.
2. Run `prek run --all-files` run to ensure linting passes.

## Pull Requests

- Keep changes focused and atomic
- Ensure all tests pass
- Follow existing code style
